[package]
name = "chyma"
version = "0.1.0"
edition = "2024"
authors = ["MrTitanHearted"]
about = "My an abomination of a programming language compiler toolset: like Chymeria, it is a mess of everything!"

[dependencies]
clap = { version = "4.5.45", features = ["derive"] }
